<form method="post" action="{{ path('app_form_schema_delete', {'id': form_schema.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form_schema.id) }}">
    <button class="btn">Delete</button>
</form>
