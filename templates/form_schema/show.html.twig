{% extends 'base.html.twig' %}

{% block title %}FormSchema{% endblock %}

{% block body %}
    <h1>FormSchema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_schema.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ form_schema.name }}</td>
            </tr>
            <tr>
                <th>DisplayName</th>
                <td>{{ form_schema.displayName }}</td>
            </tr>
            <tr>
                <th>Visiblity</th>
                <td>{{ form_schema.visiblity ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_form_schema_index') }}">back to list</a>

    <a href="{{ path('app_form_schema_edit', {'id': form_schema.id}) }}">edit</a>

    {{ include('form_schema/_delete_form.html.twig') }}
{% endblock %}
